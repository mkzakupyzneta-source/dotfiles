# Program → jego pliki ustawień (ścieżki względem katalogu domowego). Spec rozdz. 7.2.
# Po co ten plik, skoro chezmoi zna listę? Żeby apka umiała powiedzieć
# "usuwasz Flameshota — usunąć też jego ustawienia?" i "ustawienia X są nowsze na Vostro".
#
# Format:  <program>  <ścieżka> [<ścieżka> …]
# Adnotacja `# nie wozimy — decyzja usera <data>` = apka nie pyta drugi raz.

# --- wożone przez chezmoi (sprawdzone `chezmoi managed`, 23.08) ---
# kitty / wezterm — ZDJĘTE Z LUSTRA na gałęzi `ubuntu-2604` (werdykt usera [420], 14.09):
#   terminalem na nowej maszynie jest GNOME Terminal (jego ustawienia jadą w warstwie
#   pulpitu, gałąź /org/gnome/terminal/legacy/ w pulpit/pulpit.ini). Pliki
#   .config/kitty/kitty.conf i .config/wezterm/wezterm.lua nie są już wożone.
# onedrive (domyślna instancja) — ZDJĘTE Z LUSTRA 2026-08-23 (E2). Obszar 4_Kanaly_danych
#   ustalił, że ~/.config/onedrive (folder ~/OneDrive-AI) to zaszłość: katalog przemianowany
#   na ~/.config/onedrive.ZASZLOSC-2026-08-23, usługa onedrive.service zamaskowana.
#   `chezmoi forget` na .config/onedrive/{config,sync_list} + bin/onedrive-roboczy.sh.
#   Żywa jest wyłącznie instancja onedrive-archiwum (wiersz niżej).
onedrive-archiwum .config/onedrive-archiwum/config .config/onedrive-archiwum/sync_list
ccstatusline     .config/ccstatusline/settings.json
git              .gitconfig
powloka          .bashrc .profile
# gnome-pulpit .config/zrzuty/gnome-dconf.ini — ZDJĘTE na gałęzi `ubuntu-2604` ([423], 14.09):
#   to było TRZECIE miejsce prawdy o pulpicie (obok lustra/pulpit/pulpit.ini i ubuntu-profil/).
#   Jedynym kanonem pulpitu jest teraz lustra/pulpit/pulpit.ini; zrzut z ~/.config/zrzuty
#   zlikwidowany razem z plikiem.

flameshot        .config/flameshot/flameshot.ini    # skróty w programie: TYPE_ACCEPT=S
#   ^ dołożone do chezmoi 2026-08-23 w E2 (do tego dnia plik istniał na maszynie,
#     ale chezmoi go NIE woził — hipoteza 17 ze spec obalona w E1).

# --- decyzje usera z 23.08 ---
# [126] Chrome i VS Code: NIE lustrzymy plików ustawień — mają własną synchronizację
#       przez konto (Chrome Sync, VS Code Settings Sync). W lustrze zostaje sam program.
google-chrome    # nie wozimy — decyzja usera [126] 2026-08-23 (własna synchronizacja konta)
code             # nie wozimy — decyzja usera [126] 2026-08-23 (Settings Sync)

# [125] wchodzą do lustra:
ssh-aliasy       .ssh/config                        # ⛔ TYLKO config, klucze prywatne NIGDY (7.6)
zakladki-plikow  .config/gtk-3.0/bookmarks          # zakładki menedżera plików i udziały smb://
czcionki-wlasne  .local/share/fonts
autostart        .config/autostart
uslugi-uzytkownika .config/systemd/user
forticlient          # nie wozimy — decyzja usera 2026-08-25
ailauncher           # nie wozimy — decyzja usera 2026-08-29
