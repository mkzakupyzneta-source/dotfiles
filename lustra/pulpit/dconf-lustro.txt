# Ścieżki GNOME (dconf) objęte lustrem — spec rozdz. 8.3.
# Jedna ścieżka na linię, `#` = komentarz.
# Eksportujemy WYBRANE ścieżki, nie `dconf dump /` — uzasadnienie w spec 8.2.

# --- skróty klawiszowe ---
/org/gnome/settings-daemon/plugins/media-keys/   # WŁASNE skróty usera (custom0, custom1…)
/org/gnome/desktop/wm/keybindings/               # skróty okien (na Vostro puste = same domyślne)
/org/gnome/shell/keybindings/                    # skróty powłoki (na Vostro: Print → zrzut GNOME)
/org/gnome/mutter/keybindings/                   # kafelkowanie okien

# --- klawiatura (decyzja [124] z 23.08 — WCHODZI do lustra) ---
/org/gnome/desktop/input-sources/                # układ klawiatury i klawisz Compose
                                                 # na Vostro: sources=[('xkb','pl')], xkb-options=[]

# --- wygląd ---
/org/gnome/desktop/interface/                    # motyw, ikony, CZCIONKI systemowe
/org/gnome/desktop/background/                   # tapeta (samo ustawienie; plik osobno → 8.6)
/org/gnome/desktop/screensaver/                  # wygaszacz: kolor, blokada
/org/gnome/shell/extensions/dash-to-dock/        # dock: pozycja, kolor, rozmiar ikon
/org/gnome/desktop/wm/preferences/               # układ przycisków okna (na Vostro: :minimize,maximize,close)

# --- zachowanie pulpitu ---
/org/gnome/mutter/                               # np. edge-tiling
/org/gnome/desktop/session/                      # po ilu minutach bezczynności
/org/gnome/nautilus/preferences/                 # menedżer plików: widok, sortowanie

# --- programy z ustawieniami w dconf ---
/org/gnome/terminal/legacy/                      # GNOME Terminal: profile (m.in. czarny profil
                                                 # z Vostro — user zauwazyl brak na Katanie 25.08,
                                                 # poprawka 9 planu automatu), skroty, menu.
                                                 # Wejdzie do wzorca przy najblizszym
                                                 # pulpit-oddaj na Vostro (recznym, [195]).

# --- telefon w komputerze: GSConnect (KDE Connect dla GNOME), sprawa [299] ---
# WOZIMY WYŁĄCZNIE USTAWIENIA WTYCZEK dla JEDNEGO urządzenia — telefonu. Reszta gałęzi
# `gsconnect` to TOŻSAMOŚĆ MASZYNY i STAN PAROWANIA (certyfikat drugiej strony, `paired`,
# `last-connection`, nazwa maszyny, lista `devices`) — tego NIE WOLNO kopiować między
# lustrami: skopiowany certyfikat rozwala parowanie, skopiowana nazwa robi dwie maszyny
# o tej samej tożsamości. Dlatego ścieżka celuje w `.../device/<ID TELEFONU>/plugin/`,
# a nie w całe `/org/gnome/shell/extensions/gsconnect/`.
#
# <ID TELEFONU> = identyfikator urządzenia nadany przez apkę KDE Connect NA TELEFONIE.
# Jest TEN SAM na każdym komputerze, który ten telefon widzi (zmierzone 31.08 na hp i vostro),
# więc jest zwykłą stałą danych — nie znacznikiem maszynowym. Przeinstalowanie KDE Connect
# na telefonie = nowy identyfikator = zmiana TEJ JEDNEJ linii (i dwóch niżej: w
# dconf-wyjatki.txt i dconf-pomijane-klucze.txt).
#   c7746d3650f644c1aca106b623fe8a98 = POCO X7 Pro
/org/gnome/shell/extensions/gsconnect/device/c7746d3650f644c1aca106b623fe8a98/plugin/
